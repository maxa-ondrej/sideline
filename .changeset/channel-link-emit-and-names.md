---
'@sideline/domain': patch
'@sideline/server': patch
---

Emit channel_created event when linking an existing channel so the bot creates a role, and show channel names instead of raw snowflake IDs
