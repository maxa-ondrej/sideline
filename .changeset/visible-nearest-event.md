---
"@sideline/bot": patch
"@sideline/server": patch
"@sideline/domain": patch
"@sideline/migrations": patch
"@sideline/i18n": patch
---

Reorder attendance channel messages so the nearest upcoming event is the last (most visible) message, and add a divider between past and future events
