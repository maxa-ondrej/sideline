---
'@sideline/bot': patch
'@sideline/server': patch
'@sideline/domain': patch
'@sideline/i18n': patch
---

RSVP now saves immediately on button click. Ephemeral confirmation shows "Add a message" button (or "Edit message" + "Clear message" if a message already exists). Message is preserved when re-clicking the same RSVP button.
