use flake;

# Use the exact pnpm version specified in packageManager
pnpm() {
  command npx pnpm@10.14.0 "$@"
}
export -f pnpm

git config core.hooksPath .husky

export DATABASE_URL=postgresql://sideline:sideline@localhost:5432/sideline
