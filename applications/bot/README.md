# @sideline/bot

Discord bot for Sideline, built with [dfx](https://github.com/tim-smart/dfx) (Effect-native Discord library).

## Running

Set the `DISCORD_BOT_TOKEN` environment variable, then run:

```sh
pnpm --filter @sideline/bot dev
```
