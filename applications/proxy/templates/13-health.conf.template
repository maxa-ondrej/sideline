listen $HEALTH_PORT;

location = /healthz {
    default_type application/json;
    return 200 '{"status":"ok"}';
}

location = /info {
    default_type application/json;
    return 200 '{"version":"${APP_VERSION}","commit":"${GIT_COMMIT}","build_time":"${BUILD_TIME}"}';
}
